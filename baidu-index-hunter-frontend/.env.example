# API 配置（可选，未设置时使用默认值）
# 开发环境：使用相对路径 /api 会走 Vite 代理
# 生产环境：若前后端同域，使用 /api；若分离部署，填写完整地址
VITE_API_BASE_URL=/api

# WebSocket 地址（可选，未设置时自动推导）
# 开发环境：自动指向同主机 5001 端口
# 生产环境：同域则自动使用当前域名
# VITE_SOCKET_URL=http://127.0.0.1:5001

# Vite 开发服务器代理目标（可选，仅影响 npm run dev）
# VITE_API_PROXY_TARGET=http://127.0.0.1:5001
